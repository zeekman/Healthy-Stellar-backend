<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>New Record Available</title>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; background: #f5f7fa; margin: 0; }
    .wrapper { max-width: 600px; margin: 40px auto; background: #fff; border-radius: 8px; overflow: hidden; box-shadow: 0 2px 8px rgba(0,0,0,.08); }
    .header { background: #8b5cf6; padding: 32px; text-align: center; }
    .header h1 { color: #fff; margin: 0; font-size: 22px; font-weight: 600; }
    .body { padding: 32px; color: #374151; line-height: 1.6; }
    .card { background: #faf5ff; border-left: 4px solid #8b5cf6; border-radius: 4px; padding: 16px; margin: 24px 0; }
    .card p { margin: 4px 0; font-size: 14px; }
    .footer { background: #f9fafb; padding: 24px; text-align: center; font-size: 12px; color: #6b7280; }
    .footer a { color: #6b7280; }
  </style>
</head>
<body>
  <div class="wrapper">
    <div class="header">
      <h1>📄 New Record Available</h1>
    </div>
    <div class="body">
      <p>Hi <strong>{{patientName}}</strong>,</p>
      <p>A new medical record has been added to your account:</p>

      <div class="card">
        <p><strong>Record:</strong> {{recordTitle}}</p>
        {{#if recordType}}
        <p><strong>Type:</strong> {{recordType}}</p>
        {{/if}}
        <p><strong>Uploaded by:</strong> {{uploadedBy}}</p>
        <p><strong>Date:</strong> {{uploadedAt}}</p>
      </div>

      <p style="text-align:center; margin-top: 32px;">
        <a href="{{viewRecordUrl}}" style="background:#8b5cf6;color:#fff;padding:12px 28px;border-radius:6px;text-decoration:none;font-weight:600;">
          View Record
        </a>
      </p>
    </div>
    <div class="footer">
      <p>You received this because you have an active HealthRecord account.</p>
      <p><a href="{{unsubscribeUrl}}">Unsubscribe from email notifications</a></p>
    </div>
  </div>
</body>
</html>
